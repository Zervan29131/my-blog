# 个人博客系统测试需求

## 1. 测试目标

对已完成的个人博客系统进行完整测试，确认系统满足以下目标：

1. 博客前台可以正常浏览文章和提交评论。
2. 管理员可以正常登录并管理文章、评论。
3. 前后端接口行为符合需求约定。
4. 权限控制、输入校验和敏感信息保护有效。
5. Docker Compose 可以稳定启动和运行。
6. PostgreSQL 数据可以持久化保存。
7. 项目可以从本地环境迁移到 Linux 服务器。
8. 系统不存在阻塞发布的严重缺陷。

测试过程中不得新增业务功能。发现问题时，只记录和修复与现有需求不符的行为。

---

## 2. 被测系统

### 2.1 技术架构

* 前端：Vue 3、TypeScript、Vite
* 后端：Go、Gin、GORM
* 数据库：PostgreSQL
* 网关：Nginx
* 部署：Docker Compose
* 接口：REST API
* 认证：JWT
* 内容格式：Markdown

### 2.2 核心业务模块

测试范围包括：

* 博客首页
* 文章详情
* 评论提交
* 管理员登录
* 后台统计
* 文章管理
* 评论管理
* API 接口
* JWT 鉴权
* Docker 部署
* 数据持久化
* 安全校验
* 基础性能和稳定性

---

## 3. 测试范围

### 3.1 包含范围

#### 博客前台

* 已发布文章列表
* 文章分页
* 文章详情
* Markdown 内容渲染
* 评论列表
* 评论提交
* 页面加载状态
* 页面错误提示
* 移动端基础适配

#### 管理后台

* 管理员登录
* 登录状态保持
* 登录过期处理
* 后台路由保护
* 后台统计
* 文章创建
* 文章编辑
* 草稿和发布状态切换
* 文章删除
* 评论查询
* 评论状态筛选
* 评论审核
* 评论拒绝
* 评论删除

#### 后端接口

* 参数校验
* HTTP 状态码
* 返回数据结构
* 分页逻辑
* JWT 鉴权
* Slug 唯一性
* 评论状态控制
* 错误响应
* 敏感字段保护
* 评论限流

#### 部署

* Docker 镜像构建
* Docker Compose 启动
* 容器依赖关系
* Nginx 代理
* Vue History 路由
* PostgreSQL 健康检查
* PostgreSQL 数据持久化
* 环境变量检查
* Linux 服务器可移植性

---

### 3.2 不包含范围

本次测试不要求验证以下未实现功能：

* 用户注册
* 多管理员
* 权限角色
* 分类和标签
* 站内搜索
* 图片上传
* 评论回复
* 嵌套评论
* 邮件通知
* 验证码
* OAuth
* Redis
* 消息队列
* 微服务
* Kubernetes
* 自动化 HTTPS 证书
* 浏览量
* 点赞和收藏
* 国际化
* SEO 高级优化

如果测试过程中发现上述功能不存在，不视为缺陷。

---

## 4. 测试环境

### 4.1 本地环境

推荐测试环境：

```text
操作系统：Windows、macOS 或 Linux
Docker：最新稳定版
Docker Compose：V2
浏览器：Chrome 最新稳定版
内存：至少 4 GB
可用磁盘：至少 5 GB
```

### 4.2 测试地址

默认地址：

```text
前台：http://localhost:8080
后台：http://localhost:8080/admin
健康检查：http://localhost:8080/api/v1/health
```

### 4.3 测试环境变量

必须使用专用测试账号和测试数据库。

示例：

```env
APP_ENV=test

POSTGRES_DB=personal_blog_test
POSTGRES_USER=blog_test
POSTGRES_PASSWORD=test_password

ADMIN_USERNAME=test_admin
ADMIN_PASSWORD=TestPassword123!
JWT_SECRET=test_jwt_secret_at_least_32_characters_long
JWT_EXPIRES_HOURS=24

CORS_ALLOWED_ORIGINS=http://localhost:8080,http://localhost:5173
```

禁止在测试报告中记录真实生产密码和 JWT Secret。

---

## 5. 测试数据准备

测试前至少准备以下数据。

### 5.1 管理员

```text
用户名：test_admin
密码：TestPassword123!
```

### 5.2 文章数据

#### 文章 A

```text
标题：测试已发布文章
Slug：published-test-article
状态：published
摘要：用于验证已发布文章展示
正文：包含标题、段落、列表、代码块和链接
```

#### 文章 B

```text
标题：测试草稿文章
Slug：draft-test-article
状态：draft
摘要：用于验证草稿不可公开访问
正文：草稿测试正文
```

#### 文章 C

```text
标题：测试删除文章
Slug：delete-test-article
状态：published
正文：用于测试级联删除评论
```

### 5.3 评论数据

至少准备：

* 1 条 pending 评论
* 1 条 approved 评论
* 1 条 rejected 评论
* 1 条包含邮箱的评论
* 1 条即将随文章删除的评论

---

## 6. 测试类型

本项目应执行以下测试：

1. 冒烟测试
2. 功能测试
3. API 测试
4. 权限测试
5. 数据库测试
6. 安全测试
7. 异常测试
8. 兼容性测试
9. 部署测试
10. 基础性能测试
11. 回归测试
12. 最终验收测试

---

# 7. 冒烟测试

冒烟测试用于确认系统具备基本可测试条件。

## SMOKE-001 服务启动

### 前置条件

Docker 已启动。

### 操作

```bash
docker compose up -d --build
docker compose ps
```

### 预期结果

* 所有容器成功启动。
* PostgreSQL 状态正常。
* Backend 状态正常。
* Frontend 或 Nginx 状态正常。
* 不存在持续重启的容器。

---

## SMOKE-002 健康检查

### 操作

```bash
curl http://localhost:8080/api/v1/health
```

### 预期结果

* HTTP 状态码为 `200`。
* 返回内容包含：

```json
{
  "data": {
    "status": "ok"
  },
  "message": "success"
}
```

---

## SMOKE-003 前台访问

### 操作

浏览器访问：

```text
http://localhost:8080
```

### 预期结果

* 页面可以打开。
* 不出现空白页面。
* 不出现 JavaScript 致命错误。
* 页面可以显示文章列表或无数据状态。

---

## SMOKE-004 后台登录

### 操作

使用有效管理员账号登录。

### 预期结果

* 登录成功。
* 跳转到后台首页。
* 后台统计接口正常返回。
* 页面刷新后登录状态保持。

---

# 8. 前台文章测试

## ARTICLE-PUBLIC-001 获取文章列表

### 操作

访问博客首页。

### 预期结果

* 只显示 `published` 状态文章。
* 草稿文章不显示。
* 每篇文章显示标题、摘要、发布时间和评论数。
* 不在列表中返回完整正文。

---

## ARTICLE-PUBLIC-002 文章分页

### 前置条件

创建超过 10 篇已发布文章。

### 操作

访问第一页和第二页。

### 预期结果

* 默认每页显示 10 篇。
* 翻页后文章内容变化。
* 总数和总页数正确。
* 不出现重复文章。
* 不遗漏文章。

---

## ARTICLE-PUBLIC-003 非法分页参数

测试以下参数：

```text
page=0
page=-1
page=abc
page_size=0
page_size=1000
page_size=abc
```

### 预期结果

系统应采用以下任一合理策略：

* 返回 `400` 参数错误；或
* 使用安全默认值。

不得出现：

* 500 错误
* 数据库错误
* 无限数据返回
* 服务崩溃

---

## ARTICLE-PUBLIC-004 查看已发布文章

### 操作

访问：

```text
/articles/published-test-article
```

### 预期结果

* 页面成功显示。
* 标题正确。
* 正文正确。
* Markdown 可以正常渲染。
* 发布时间正确。
* 评论区域正常显示。

---

## ARTICLE-PUBLIC-005 访问草稿文章

### 操作

直接访问草稿文章 Slug。

### 预期结果

* 返回 404 页面或资源不存在提示。
* 不泄露草稿正文。
* 不显示草稿标题和摘要。

---

## ARTICLE-PUBLIC-006 访问不存在的文章

### 操作

访问：

```text
/articles/not-exist
```

### 预期结果

* 返回友好的 404 页面或错误提示。
* 前端不崩溃。
* 后端返回 HTTP 404。
* 不返回内部错误信息。

---

## ARTICLE-PUBLIC-007 Markdown 渲染

文章正文至少包含：

````markdown
# 一级标题

普通段落

- 列表一
- 列表二

```go
fmt.Println("hello")
````

[测试链接](https://example.com)

````

### 预期结果

- 标题正确渲染。
- 段落正确渲染。
- 列表正确渲染。
- 代码块正确显示。
- 链接可点击。
- 页面结构不混乱。

---

## ARTICLE-PUBLIC-008 Markdown XSS 清理

文章正文中加入：

```html
<script>alert('xss')</script>
<img src="x" onerror="alert('xss')">
<a href="javascript:alert('xss')">test</a>
````

### 预期结果

* JavaScript 不执行。
* 危险标签或属性被移除。
* 页面不弹出提示框。
* 页面其他内容正常显示。

---

# 9. 评论功能测试

## COMMENT-001 提交有效评论

### 请求数据

```json
{
  "nickname": "测试访客",
  "email": "visitor@example.com",
  "content": "这是一条测试评论。"
}
```

### 预期结果

* HTTP 状态码为 201 或符合实现约定的成功状态码。
* 页面显示提交成功提示。
* 评论状态为 `pending`。
* 评论不会立即出现在公开评论列表中。

---

## COMMENT-002 不填写邮箱

### 请求数据

```json
{
  "nickname": "测试访客",
  "email": "",
  "content": "不填写邮箱的评论。"
}
```

### 预期结果

* 评论提交成功。
* 邮箱字段为空不会导致错误。
* 评论状态为 pending。

---

## COMMENT-003 昵称为空

### 请求数据

```json
{
  "nickname": "",
  "email": "",
  "content": "测试内容"
}
```

### 预期结果

* 返回 HTTP 400。
* 显示明确的昵称必填提示。
* 不创建评论记录。

---

## COMMENT-004 昵称长度不足

测试昵称：

```text
A
```

### 预期结果

* 返回 HTTP 400。
* 不创建评论。

---

## COMMENT-005 昵称长度超过限制

提交超过 50 个字符的昵称。

### 预期结果

* 返回 HTTP 400。
* 不创建评论。
* 服务不崩溃。

---

## COMMENT-006 评论内容为空

### 预期结果

* 返回 HTTP 400。
* 不创建评论。
* 显示内容必填提示。

---

## COMMENT-007 评论内容超过限制

提交超过 1000 个字符的评论。

### 预期结果

* 返回 HTTP 400。
* 不创建评论。
* 不发生数据库字段截断后仍返回成功的情况。

---

## COMMENT-008 邮箱格式错误

提交：

```text
not-an-email
```

### 预期结果

推荐行为：

* 返回 HTTP 400；或
* 若系统需求只限制长度，则允许保存。

测试结果应与当前接口文档一致，不得前后端校验不一致。

---

## COMMENT-009 评论 XSS

提交以下内容：

```html
<script>alert('comment-xss')</script>
<img src="x" onerror="alert('comment-xss')">
```

### 预期结果

* 评论展示时 JavaScript 不执行。
* 评论应以纯文本显示，或经过安全清理。
* 不影响页面其他内容。

---

## COMMENT-010 公开评论列表

### 前置条件

存在 pending、approved、rejected 三种评论。

### 预期结果

* 只显示 approved 评论。
* pending 评论不显示。
* rejected 评论不显示。
* 返回结果中不包含 email。
* 返回结果中不包含内部状态字段，除非接口设计明确需要。

---

## COMMENT-011 评论排序

创建多条已审核评论。

### 预期结果

评论顺序应稳定，并符合系统定义：

* 按创建时间升序；或
* 按创建时间降序。

同一页面重复访问时不得随机变化。

---

## COMMENT-012 评论限流

在同一 IP 下，一分钟内连续提交 6 次评论。

### 预期结果

* 前 5 次按正常业务处理。
* 第 6 次返回 HTTP 429。
* 返回明确的请求过于频繁提示。
* 服务不崩溃。
* 限流不影响普通文章浏览。

---

# 10. 管理员认证测试

## AUTH-001 正确账号登录

### 预期结果

* 登录成功。
* 返回 JWT。
* 返回过期时间。
* JWT 不为空。
* 密码不出现在响应中。

---

## AUTH-002 错误密码登录

### 预期结果

* 返回 HTTP 401。
* 不返回 JWT。
* 错误提示不得暴露密码校验细节。
* 日志不得记录明文密码。

---

## AUTH-003 不存在的管理员

### 预期结果

* 返回 HTTP 401。
* 提示内容与错误密码保持一致或相近。
* 不泄露账号是否存在。

---

## AUTH-004 缺少用户名

### 预期结果

* 返回 HTTP 400。
* 不执行成功登录。

---

## AUTH-005 缺少密码

### 预期结果

* 返回 HTTP 400。
* 不执行成功登录。

---

## AUTH-006 无 Token 访问后台接口

### 操作

请求：

```http
GET /api/v1/admin/articles
```

不携带 Authorization。

### 预期结果

* 返回 HTTP 401。
* 不返回后台数据。

---

## AUTH-007 无效 Token

使用随机字符串作为 Token。

### 预期结果

* 返回 HTTP 401。
* 服务不崩溃。
* 不返回 JWT 解析内部错误。

---

## AUTH-008 过期 Token

使用已经过期的 JWT。

### 预期结果

* 返回 HTTP 401。
* 前端删除本地 Token。
* 前端跳转到登录页。
* 不出现无限请求循环。

---

## AUTH-009 错误 Authorization 格式

测试：

```text
Authorization: Token abc
Authorization: Bearer
Authorization: abc
```

### 预期结果

* 返回 HTTP 401。
* 不返回 500。

---

## AUTH-010 后台路由保护

### 操作

清除 localStorage 中 Token，直接访问：

```text
/admin/articles
```

### 预期结果

* 自动跳转到 `/admin/login`。
* 后台页面不短暂展示敏感数据。
* 登录成功后可以正常访问后台。

---

# 11. 后台统计测试

## DASHBOARD-001 数据统计准确性

准备：

* 3 篇文章
* 2 篇 published
* 1 篇 draft
* 2 条 pending 评论
* 3 条 approved 评论

### 预期结果

返回：

```text
article_total = 3
article_published = 2
article_draft = 1
comment_pending = 2
comment_approved = 3
```

---

## DASHBOARD-002 删除数据后统计更新

删除一篇文章及其评论。

### 预期结果

* 文章数量减少。
* 相关评论数量减少。
* 页面刷新后统计正确。
* 不显示旧缓存数据。

---

# 12. 后台文章管理测试

## ARTICLE-ADMIN-001 创建草稿文章

### 输入

```text
标题：新建草稿文章
Slug：new-draft-article
摘要：测试摘要
正文：测试正文
状态：draft
```

### 预期结果

* 创建成功。
* 后台列表可见。
* 前台列表不可见。
* 公开详情接口返回 404。

---

## ARTICLE-ADMIN-002 创建已发布文章

### 预期结果

* 创建成功。
* 状态为 published。
* 自动设置 published_at。
* 前台列表可见。
* 文章详情可访问。

---

## ARTICLE-ADMIN-003 标题为空

### 预期结果

* 返回 HTTP 400。
* 不创建文章。
* 页面显示明确提示。

---

## ARTICLE-ADMIN-004 正文为空

### 预期结果

* 返回 HTTP 400。
* 不创建文章。

---

## ARTICLE-ADMIN-005 Slug 格式错误

测试：

```text
Test Article
测试文章
test_article
test/article
TEST-ARTICLE
```

### 预期结果

* 返回 HTTP 400。
* 明确提示 Slug 只能包含小写字母、数字和连字符。

---

## ARTICLE-ADMIN-006 Slug 重复

创建两个相同 Slug 的文章。

### 预期结果

* 第一篇创建成功。
* 第二篇返回 HTTP 409。
* 不覆盖原文章。
* 错误提示明确。

---

## ARTICLE-ADMIN-007 编辑文章

修改：

* 标题
* 摘要
* 正文

### 预期结果

* 修改成功。
* Slug 未修改时文章地址不变。
* 前台显示最新内容。
* updated_at 更新。

---

## ARTICLE-ADMIN-008 草稿发布

将 draft 修改为 published。

### 预期结果

* 状态更新成功。
* 设置 published_at。
* 文章出现在前台列表。
* 可以通过 Slug 访问。

---

## ARTICLE-ADMIN-009 已发布文章改回草稿

### 预期结果

* 状态更新为 draft。
* 前台列表不再显示。
* 原公开地址返回 404。
* 后台仍可查看和编辑。

---

## ARTICLE-ADMIN-010 已发布文章再次编辑

### 预期结果

* 更新正文后仍为 published。
* published_at 保持原值。
* updated_at 更新。
* 前台显示更新后的内容。

---

## ARTICLE-ADMIN-011 删除文章取消操作

### 操作

点击删除，在确认框中取消。

### 预期结果

* 文章未删除。
* 页面数据保持不变。
* 不发送删除请求，或删除请求未生效。

---

## ARTICLE-ADMIN-012 删除文章确认操作

### 预期结果

* 文章删除成功。
* 后台列表不再显示。
* 前台访问返回 404。
* 关联评论同步删除。

---

## ARTICLE-ADMIN-013 删除不存在的文章

### 预期结果

* 返回 HTTP 404。
* 不返回 500。
* 不影响其他文章。

---

# 13. 后台评论管理测试

## COMMENT-ADMIN-001 查看全部评论

### 预期结果

* 能查看所有状态评论。
* 显示所属文章。
* 显示昵称、内容、状态和时间。
* 邮箱仅在后台按设计显示。
* 不显示数据库内部字段。

---

## COMMENT-ADMIN-002 按 pending 筛选

### 预期结果

* 只显示 pending 评论。
* 分页总数正确。

---

## COMMENT-ADMIN-003 审核通过评论

### 操作

将 pending 修改为 approved。

### 预期结果

* 状态更新成功。
* 评论出现在文章前台。
* 评论内容正确。
* 邮箱不出现在前台。

---

## COMMENT-ADMIN-004 拒绝评论

将 pending 修改为 rejected。

### 预期结果

* 状态更新成功。
* 评论不出现在前台。
* 后台可以按 rejected 查询。

---

## COMMENT-ADMIN-005 非法评论状态

提交：

```json
{
  "status": "deleted"
}
```

### 预期结果

* 返回 HTTP 400。
* 原评论状态不变。

---

## COMMENT-ADMIN-006 删除评论

### 预期结果

* 评论删除成功。
* 后台列表不再显示。
* 前台不再显示该评论。
* 其他评论不受影响。

---

## COMMENT-ADMIN-007 操作不存在的评论

对不存在的评论执行状态更新或删除。

### 预期结果

* 返回 HTTP 404。
* 不返回 500。

---

# 14. API 规范测试

## API-001 成功响应格式

成功接口必须使用统一结构：

```json
{
  "data": {},
  "message": "success"
}
```

---

## API-002 分页响应格式

分页接口必须包含：

```text
items
page
page_size
total
total_pages
```

检查：

* 字段类型正确。
* total_pages 计算正确。
* 空列表时 items 为数组，不是 null。

---

## API-003 错误响应格式

错误接口应返回：

```json
{
  "error": {
    "code": "ERROR_CODE",
    "message": "错误说明",
    "details": {}
  }
}
```

检查：

* code 非空。
* message 可理解。
* 不包含调用栈。
* 不包含 SQL。
* 不包含环境变量。

---

## API-004 Content-Type

JSON 接口响应头应包含：

```text
Content-Type: application/json
```

---

## API-005 HTTP 方法限制

对接口使用错误方法，例如：

```http
POST /api/v1/articles
DELETE /api/v1/health
```

### 预期结果

* 返回 404 或 405。
* 不执行数据修改。
* 不返回 500。

---

## API-006 超大请求体

提交明显超过合理范围的请求体。

### 预期结果

* 请求被拒绝。
* 返回 400 或 413。
* 服务不崩溃。
* 内存不持续异常增长。

---

# 15. 数据库测试

## DB-001 自动建表

使用全新数据库启动系统。

### 预期结果

* 必需的数据表成功创建。
* 服务可以正常启动。
* 管理员初始化成功。

---

## DB-002 管理员密码存储

查看 administrators 表。

### 预期结果

* 不存在明文密码。
* password_hash 为 bcrypt 哈希。
* 哈希值与原密码不同。

---

## DB-003 唯一约束

验证：

* 管理员用户名唯一
* 文章 Slug 唯一

### 预期结果

* 数据库层面存在唯一约束。
* 冲突时接口返回合理错误。
* 不产生重复记录。

---

## DB-004 文章删除级联评论

创建文章和评论后删除文章。

### 预期结果

* 文章被删除。
* 关联评论被删除。
* 不产生孤立评论。

---

## DB-005 数据持久化

### 操作

```bash
docker compose restart
```

或：

```bash
docker compose down
docker compose up -d
```

### 预期结果

* 已创建文章仍存在。
* 已创建评论仍存在。
* 管理员仍可以登录。
* PostgreSQL volume 正常工作。

---

## DB-006 清除数据卷

### 操作

```bash
docker compose down -v
docker compose up -d --build
```

### 预期结果

* 原测试数据被清除。
* 数据库重新初始化。
* 管理员重新创建。
* 服务正常启动。

---

# 16. 安全测试

## SECURITY-001 SQL 注入

在以下输入中测试常见注入字符串：

```text
' OR '1'='1
admin'--
1; DROP TABLE articles;
```

测试位置：

* 登录用户名
* 文章 Slug
* 评论昵称
* 评论内容
* 搜索或筛选参数

### 预期结果

* 不发生绕过登录。
* 不执行恶意 SQL。
* 数据表不被删除。
* 返回正常参数错误或无结果。

---

## SECURITY-002 JWT 篡改

修改 JWT Payload 或签名。

### 预期结果

* 返回 HTTP 401。
* 不允许访问后台。
* 不信任未经验证的角色或用户信息。

---

## SECURITY-003 敏感信息泄露

检查所有公开接口和错误响应。

不得出现：

* 数据库密码
* JWT Secret
* 管理员密码哈希
* 评论邮箱
* SQL 语句
* Go 堆栈
* 容器内部路径
* 环境变量完整内容

---

## SECURITY-004 CORS

从白名单内 Origin 发起请求。

### 预期结果

* 请求允许。

从不在白名单的 Origin 发起请求。

### 预期结果

* 不返回允许该 Origin 的 CORS 头。
* 后台接口不能被任意站点跨域调用。

---

## SECURITY-005 默认密码

使用生产模式启动，但仍使用：

```text
ADMIN_PASSWORD=change_me
```

### 预期结果

推荐行为：

* 启动失败；或
* 输出明显安全警告。

不得静默使用弱默认密码部署生产环境。

---

## SECURITY-006 日志脱敏

执行登录失败和评论提交后检查日志。

### 预期结果

日志不得包含：

* 密码
* 完整 JWT
* 评论邮箱
* JWT Secret
* 数据库密码

---

## SECURITY-007 路径遍历

测试路径：

```text
/articles/../../etc/passwd
/api/v1/articles/%2e%2e/%2e%2e/etc/passwd
```

### 预期结果

* 返回 400 或 404。
* 不返回服务器文件内容。
* 不暴露内部目录结构。

---

# 17. 前端异常测试

## FRONTEND-001 API 服务不可用

停止 Backend 后访问前台。

### 预期结果

* 页面显示友好的加载失败提示。
* 不永久停留在加载状态。
* 不出现未捕获异常导致的白屏。

---

## FRONTEND-002 网络超时

模拟慢速网络或请求超时。

### 预期结果

* 页面显示失败或重试提示。
* 按钮不会永久处于提交中。
* 评论不会因重复点击创建多条重复记录。

---

## FRONTEND-003 重复提交

快速连续点击评论提交按钮。

### 预期结果

* 提交过程中按钮禁用。
* 不产生重复评论，或系统可以合理处理重复请求。
* 页面不会连续弹出大量提示。

---

## FRONTEND-004 表单错误提示

提交非法文章或评论数据。

### 预期结果

* 错误提示显示在对应字段附近或明确位置。
* 修正数据后可以再次提交。
* 不需要刷新页面恢复。

---

## FRONTEND-005 页面刷新

分别刷新：

```text
/articles/published-test-article
/admin
/admin/articles
/admin/comments
```

### 预期结果

* 不出现 Nginx 404。
* Vue Router 正常接管路由。
* 已登录后台刷新后仍可使用。
* 未登录后台刷新后跳转到登录页。

---

# 18. 浏览器和响应式测试

## 18.1 浏览器范围

至少测试：

* Chrome 最新稳定版
* Edge 最新稳定版
* Firefox 最新稳定版

Safari 可作为可选测试项。

## 18.2 屏幕尺寸

至少测试：

```text
桌面：1440 × 900
笔记本：1366 × 768
平板：768 × 1024
手机：375 × 812
```

## 18.3 验收要求

* 页面不出现横向溢出。
* 文字可正常阅读。
* 按钮可正常点击。
* 表单不超出屏幕。
* 后台表格在小屏幕下可以滚动或合理压缩。
* Markdown 代码块可以横向滚动。
* 长标题不会破坏页面布局。

---

# 19. 基础性能测试

本项目不是高并发系统，只进行基础性能验证。

## PERFORMANCE-001 首页响应

### 条件

数据库中有 100 篇文章。

### 目标

* 本地环境首页 API 平均响应时间低于 500 毫秒。
* 单次请求不应超过 2 秒。
* 页面可正常渲染。

---

## PERFORMANCE-002 文章详情

### 条件

文章正文约 50 KB。

### 目标

* 接口正常返回。
* 页面不明显卡死。
* Markdown 渲染可完成。
* 浏览器主线程不长时间无响应。

---

## PERFORMANCE-003 并发浏览

使用 20 个并发请求访问公开文章列表和详情。

### 预期结果

* 服务不崩溃。
* 不出现大量 500。
* 数据库连接正常。
* 请求结束后服务仍可继续使用。

---

## PERFORMANCE-004 评论限流下稳定性

快速提交大量评论请求。

### 预期结果

* 超过限制后返回 429。
* Backend 不崩溃。
* CPU 和内存不持续异常增长。
* 普通文章请求仍可响应。

---

# 20. Docker 和部署测试

## DEPLOY-001 全新环境构建

### 操作

清除已有镜像和数据后执行：

```bash
docker compose down -v
docker compose build --no-cache
docker compose up -d
```

### 预期结果

* 构建成功。
* 所有服务启动成功。
* 不依赖宿主机已有 Node.js 或 Go。
* 健康检查正常。

---

## DEPLOY-002 服务依赖

### 预期结果

* Backend 在数据库可用后正常启动。
* 数据库未就绪时 Backend 不应永久崩溃重启。
* Backend 应有合理重试或启动依赖机制。

---

## DEPLOY-003 Nginx API 代理

### 操作

访问：

```text
http://localhost:8080/api/v1/health
```

### 预期结果

* 请求正确转发到 Backend。
* 不需要直接访问 Backend 端口。
* 响应头和状态码正确。

---

## DEPLOY-004 Vue History 回退

直接访问任意前端路由。

### 预期结果

Nginx 返回 `index.html`，由 Vue Router 处理页面。

---

## DEPLOY-005 PostgreSQL 端口暴露

检查 Docker Compose 配置。

### 预期结果

* PostgreSQL 默认不暴露到公网。
* 只允许 Docker 内部服务访问。
* 如本地调试暴露端口，应在生产配置中关闭。

---

## DEPLOY-006 环境变量缺失

分别移除：

* DATABASE_URL
* ADMIN_USERNAME
* ADMIN_PASSWORD
* JWT_SECRET

### 预期结果

* 应用输出明确错误。
* 应用停止启动。
* 不使用不安全的隐式默认值继续运行。

---

## DEPLOY-007 Linux 服务器迁移

在一台干净 Linux 服务器上执行：

```bash
docker compose up -d --build
```

### 预期结果

* 服务成功运行。
* 前台可以访问。
* 后台可以登录。
* 数据库正常。
* 不依赖开发机器绝对路径。

---

# 21. 自动化测试要求

## 21.1 后端测试命令

必须执行：

```bash
cd backend
go test ./...
go vet ./...
```

### 必须覆盖

* 正确登录
* 错误登录
* 无 Token 鉴权
* 无效 Token 鉴权
* 创建草稿
* 草稿不可公开访问
* 发布文章
* Slug 冲突
* 提交评论
* 评论默认 pending
* pending 评论不可公开
* approved 评论公开
* 公开接口隐藏邮箱
* 非法评论状态
* 删除文章级联评论

---

## 21.2 前端检查命令

必须执行：

```bash
cd frontend
npm ci
npm run typecheck
npm run build
```

如项目已经配置测试框架，还应执行：

```bash
npm run test
```

不得为了让测试通过而删除已有测试。

---

## 21.3 Docker 验证命令

必须执行：

```bash
docker compose config
docker compose build
docker compose up -d
docker compose ps
docker compose logs --tail=200
```

---

# 22. 缺陷等级

## P0：阻塞级

满足任一情况：

* 系统完全无法启动
* 数据库数据大面积丢失
* 任意访客可进入后台
* 管理员密码或 JWT Secret 泄露
* 远程代码执行
* SQL 注入可成功执行
* 核心数据被错误删除

处理要求：

* 必须立即修复
* 修复前不得发布

---

## P1：严重级

示例：

* 管理员无法登录
* 无法创建或发布文章
* 草稿公开泄露
* 评论审核失效
* 未审核评论公开显示
* 公开接口返回评论邮箱
* 删除文章未删除关联评论
* 数据重启后丢失
* 后台接口未鉴权

处理要求：

* 发布前必须修复

---

## P2：一般级

示例：

* 分页数量不正确
* 错误提示不清晰
* 页面刷新出现异常
* 某个浏览器布局错误
* Markdown 某些格式展示异常
* 统计数据未及时更新
* 非核心接口状态码不准确

处理要求：

* 建议发布前修复
* 无法修复时需要记录影响和规避方式

---

## P3：轻微级

示例：

* 文案错误
* 间距不统一
* 非关键样式问题
* 日志格式不一致
* 极端情况下的轻微体验问题

处理要求：

* 可以进入后续版本处理

---

# 23. 缺陷记录模板

每个缺陷使用以下格式记录：

```text
缺陷编号：BUG-001
缺陷标题：
严重等级：P0 / P1 / P2 / P3
所属模块：
测试环境：
前置条件：
复现步骤：
1.
2.
3.

实际结果：
预期结果：
复现概率：必现 / 高频 / 偶发 / 仅一次
错误日志：
截图或接口响应：
初步分析：
修复状态：待处理 / 修复中 / 待验证 / 已关闭
修复提交：
回归结果：
```

缺陷标题应描述实际问题，例如：

```text
错误：草稿文章可以通过公开详情接口访问
```

不要使用：

```text
文章有问题
```

---

# 24. 回归测试要求

每次缺陷修复后，至少执行：

1. 原缺陷复现步骤。
2. 与修改模块直接相关的测试。
3. 该模块的正常流程。
4. 该模块的异常流程。
5. 相关权限测试。
6. 必要的构建和自动化测试。

示例：

修复“草稿可以公开访问”后，应回归：

* 草稿详情返回 404。
* 草稿不出现在文章列表。
* 已发布文章仍可访问。
* 后台仍可查看草稿。
* 草稿发布后可以公开访问。
* 已发布文章改回草稿后再次不可访问。

---

# 25. 最终验收清单

## 25.1 启动和部署

* [ ] Docker Compose 可以成功构建。
* [ ] 所有容器正常运行。
* [ ] 健康检查返回 200。
* [ ] 前台可以访问。
* [ ] 后台可以访问。
* [ ] PostgreSQL 数据持久化正常。
* [ ] Vue 路由刷新不出现 404。
* [ ] API 代理正常。
* [ ] 环境变量缺失时有明确错误。
* [ ] 项目可以迁移到 Linux 服务器。

## 25.2 文章功能

* [ ] 首页只显示已发布文章。
* [ ] 文章分页正常。
* [ ] 文章详情正常。
* [ ] 草稿不可公开访问。
* [ ] 管理员可以创建文章。
* [ ] 管理员可以编辑文章。
* [ ] 管理员可以发布文章。
* [ ] 管理员可以转回草稿。
* [ ] 管理员可以删除文章。
* [ ] Slug 唯一性有效。
* [ ] Markdown 安全渲染。

## 25.3 评论功能

* [ ] 访客可以提交评论。
* [ ] 评论默认 pending。
* [ ] pending 不公开显示。
* [ ] approved 可以公开显示。
* [ ] rejected 不公开显示。
* [ ] 评论输入长度校验正常。
* [ ] 评论限流正常。
* [ ] 公开接口不返回邮箱。
* [ ] 管理员可以审核评论。
* [ ] 管理员可以拒绝评论。
* [ ] 管理员可以删除评论。

## 25.4 认证和安全

* [ ] 正确账号可以登录。
* [ ] 错误密码不能登录。
* [ ] 后台接口要求 JWT。
* [ ] 无效和过期 Token 被拒绝。
* [ ] 前端 401 处理正常。
* [ ] 密码使用 bcrypt 保存。
* [ ] SQL 注入测试未发现漏洞。
* [ ] XSS 测试未发现漏洞。
* [ ] CORS 白名单正常。
* [ ] 日志不记录敏感信息。
* [ ] 错误响应不泄露内部信息。

## 25.5 质量检查

* [ ] `go test ./...` 通过。
* [ ] `go vet ./...` 通过。
* [ ] `npm run typecheck` 通过。
* [ ] `npm run build` 通过。
* [ ] 无 P0 缺陷。
* [ ] 无 P1 缺陷。
* [ ] P2 缺陷已修复或有明确说明。
* [ ] README 与实际启动方式一致。

---

# 26. 发布通过标准

博客系统只有同时满足以下条件，才能通过测试：

1. 冒烟测试全部通过。
2. 核心业务测试全部通过。
3. 自动化测试和构建全部通过。
4. 不存在 P0 缺陷。
5. 不存在 P1 缺陷。
6. 草稿和未审核评论不会公开泄露。
7. 管理后台接口均经过 JWT 鉴权。
8. 公开接口不返回评论邮箱等敏感字段。
9. 数据库重启后数据仍然存在。
10. Docker Compose 可以从全新环境启动。
11. 核心流程可以在 Chrome 和移动端尺寸下正常使用。
12. 测试报告记录完整。

---

# 27. Codex 测试执行顺序

为了降低 Codex 使用额度，按以下任务分批测试，每次只执行一个任务。

## TEST-01：静态检查和启动检查

执行：

* 检查项目结构
* 检查环境变量
* 检查 Docker Compose
* 运行前后端构建
* 启动全部服务
* 执行健康检查
* 检查容器日志

不修改业务功能，除非发现阻塞启动的问题。

---

## TEST-02：后端自动化测试

执行：

* 运行现有 Go 测试
* 检查核心业务覆盖
* 只补充缺失的核心测试
* 运行 Go Vet
* 输出失败用例和根因

不要重构无关后端代码。

---

## TEST-03：公共接口测试

测试：

* 文章列表
* 文章详情
* 草稿隔离
* 评论列表
* 评论提交
* 输入校验
* 分页
* 评论限流
* 敏感字段保护

优先使用自动化 HTTP 测试或脚本。

---

## TEST-04：管理接口测试

测试：

* 登录
* JWT
* 后台统计
* 文章 CRUD
* 状态切换
* 评论审核
* 评论拒绝
* 评论删除
* 权限控制

---

## TEST-05：前端功能测试

测试：

* 博客首页
* 文章详情
* Markdown 渲染
* 评论提交
* 登录页面
* 后台文章管理
* 后台评论管理
* 页面刷新
* 异常提示

优先检查实际浏览器行为，不只检查源代码。

---

## TEST-06：安全测试

测试：

* SQL 注入
* XSS
* JWT 篡改
* 未授权访问
* CORS
* 敏感信息泄露
* 日志脱敏
* 非法请求体

---

## TEST-07：部署和最终验收

执行：

* 清空环境重新构建
* 验证数据持久化
* 检查 Nginx
* 检查 Vue History
* 执行所有测试命令
* 汇总缺陷
* 输出最终测试报告

---

# 28. 首次交给 Codex 的测试提示词

```text
请先完整阅读：

1. docs/REQUIREMENTS.md
2. docs/TEST_REQUIREMENTS.md

不要新增业务功能，不要更换技术栈，不要进行无关重构。

现在只执行 TEST-01：静态检查和启动检查。

要求：
1. 检查当前仓库已有代码和配置。
2. 实际执行构建、Docker 启动和健康检查。
3. 记录每条命令及其结果。
4. 如果发现阻塞启动的问题，只做最小必要修复。
5. 不执行后续测试任务。
6. 最终只报告：
   - 执行的检查
   - 执行的命令
   - 通过项
   - 失败项
   - 修改文件
   - 待处理缺陷
```

---

# 29. 后续测试提示词模板

```text
请读取：

- docs/REQUIREMENTS.md
- docs/TEST_REQUIREMENTS.md

现在只执行 TEST-XX。

要求：
1. 严格限制在该测试任务范围内。
2. 实际运行测试，不通过阅读代码推测结果。
3. 不新增需求外功能。
4. 发现缺陷时先记录复现步骤、实际结果和预期结果。
5. 只有阻塞当前测试时才进行最小修复。
6. 修复后执行针对性回归。
7. 最后只输出：
   - 测试项
   - 执行命令
   - 通过数量
   - 失败数量
   - 缺陷列表
   - 修改文件
   - 回归结果
```

将 `TEST-XX` 替换为：

```text
TEST-02
TEST-03
TEST-04
TEST-05
TEST-06
TEST-07
```

---

# 30. 单个缺陷修复提示词

```text
请读取 docs/REQUIREMENTS.md 和 docs/TEST_REQUIREMENTS.md。

只修复以下缺陷：

缺陷编号：
缺陷现象：
复现步骤：
实际结果：
预期结果：
错误日志：

限制：
1. 先定位根因。
2. 只修改与该缺陷直接相关的代码。
3. 不重构无关模块。
4. 不增加新功能。
5. 保持现有接口兼容。
6. 添加或更新能够覆盖该缺陷的测试。
7. 实际运行相关测试。
8. 最后只输出根因、修改文件、测试命令和测试结果。
```
