# 个人博客前端界面改版需求

## 1. 文档目的

本文档用于指导现有个人博客前端进行界面改版。

改版目标是将博客前台调整为具有以下特征的 Vue 技术博客界面：

* 接近 Vue 官方文档的清爽技术感
* 接近 VitePress 默认主题的文档阅读体验
* 简洁、轻量、留白充分
* 适合 Markdown 长文章阅读
* 支持明亮和暗黑主题
* 桌面端具有文章目录
* 移动端具有良好的阅读体验
* 保留现有动态博客业务功能

本文档是本次前端视觉改版的唯一需求来源。

---

# 2. 核心原则

## 2.1 保留现有技术架构

现有系统仍然保持：

* Vue 3
* TypeScript
* Vite
* Vue Router
* Pinia
* Axios
* Go 后端
* REST API
* PostgreSQL

不得为了实现 VitePress 风格而：

* 将项目迁移到 VitePress
* 将动态文章改成静态 Markdown 文件
* 删除现有 Go 后端
* 改变现有文章 API
* 改变现有评论 API
* 改变现有管理员认证逻辑
* 将前后端改成服务端渲染
* 引入新的大型前端框架

本项目只模仿 VitePress 的视觉风格和阅读交互，不直接使用 VitePress 管理博客内容。

---

## 2.2 前台与后台风格分离

博客前台和管理后台采用不同设计策略。

### 博客前台

采用 Vue/VitePress 风格：

* 轻量
* 文档化
* 内容优先
* 弱化卡片边框
* 强调排版和阅读
* 自定义原生 CSS
* 避免大量 Element Plus 视觉元素

### 管理后台

继续使用 Element Plus：

* 表格
* 表单
* 对话框
* 分页
* 消息提示
* 删除确认
* 状态标签

不得将管理后台强制改造成文档页面。

后台只需要统一品牌颜色、圆角和基础导航样式。

---

# 3. 设计目标

改版后的前台应给用户以下直观感受：

1. 第一眼像一个 Vue 技术博客或技术文档站点。
2. 页面背景干净，不使用复杂纹理和大面积渐变。
3. 文章正文是页面视觉中心。
4. 标题层级清楚，代码块易于阅读。
5. 导航栏轻量、固定且不遮挡正文。
6. 桌面端可以快速查看文章目录。
7. 移动端不会因为侧栏和目录导致页面拥挤。
8. 明暗主题切换自然。
9. 页面动画克制，不影响阅读。
10. 不因视觉改版破坏现有文章和评论功能。

---

# 4. 视觉关键词

界面视觉关键词固定为：

* Vue
* VitePress
* 技术文档
* 极简
* 清晰
* 现代
* 内容优先
* 低干扰
* 高可读性
* 轻量边框
* 适度圆角
* 柔和阴影

避免以下风格：

* 企业后台风
* 大量渐变
* 玻璃拟态
* 霓虹灯
* 复杂粒子动画
* 大面积插画
* 电商卡片风
* 新闻门户密集排版
* 过多悬浮元素
* 过多彩色标签
* 所有内容都放进卡片
* 页面大面积使用 Vue Logo

---

# 5. 品牌与颜色规范

## 5.1 品牌主色

使用 Vue 风格绿色作为主品牌色。

```css
:root {
  --blog-c-brand-1: #42b883;
  --blog-c-brand-2: #33a06f;
  --blog-c-brand-3: #2c8f63;
  --blog-c-brand-soft: rgba(66, 184, 131, 0.14);
}
```

使用场景：

* 当前导航项
* 主按钮
* 链接悬停状态
* 文章标题强调
* 选中文字
* 引用块左边框
* 分页当前页
* 输入框聚焦边框
* 文章目录当前标题

主色不得大面积作为页面背景。

---

## 5.2 明亮主题

```css
:root {
  --blog-c-bg: #ffffff;
  --blog-c-bg-alt: #f6f6f7;
  --blog-c-bg-soft: #f9f9fa;
  --blog-c-divider: #e2e2e3;
  --blog-c-border: #e5e7eb;

  --blog-c-text-1: #213547;
  --blog-c-text-2: #476582;
  --blog-c-text-3: #8a8f98;

  --blog-c-code-bg: #f6f6f7;
  --blog-c-code-inline-bg: rgba(142, 150, 170, 0.14);

  --blog-c-danger: #e53935;
  --blog-c-warning: #e6a23c;
  --blog-c-success: #42b883;
}
```

---

## 5.3 暗黑主题

```css
html.dark {
  --blog-c-bg: #1b1b1f;
  --blog-c-bg-alt: #161618;
  --blog-c-bg-soft: #202127;
  --blog-c-divider: #2e2e32;
  --blog-c-border: #3c3f44;

  --blog-c-text-1: rgba(255, 255, 245, 0.92);
  --blog-c-text-2: rgba(235, 235, 245, 0.72);
  --blog-c-text-3: rgba(235, 235, 245, 0.46);

  --blog-c-code-bg: #202127;
  --blog-c-code-inline-bg: rgba(101, 117, 133, 0.24);
}
```

暗黑主题不得使用纯黑背景。

---

## 5.4 颜色使用限制

不得出现以下问题：

* 同一页面出现多个竞争性主色
* 普通正文使用纯黑色
* 暗黑模式正文使用纯白色
* 大量文字使用品牌绿色
* 每个卡片使用不同颜色
* 链接缺少悬停或聚焦状态
* 错误状态仍使用绿色
* 仅依赖颜色表达状态

---

# 6. 字体规范

## 6.1 基础字体

优先使用系统字体，不额外加载大型网络字体。

```css
--blog-font-family-base:
  Inter,
  ui-sans-serif,
  system-ui,
  -apple-system,
  BlinkMacSystemFont,
  "Segoe UI",
  "PingFang SC",
  "Hiragino Sans GB",
  "Microsoft YaHei",
  Arial,
  sans-serif;
```

中文正文必须优先保证系统兼容性和可读性。

---

## 6.2 等宽字体

```css
--blog-font-family-mono:
  ui-monospace,
  SFMono-Regular,
  Menlo,
  Monaco,
  Consolas,
  "Liberation Mono",
  "Courier New",
  monospace;
```

应用于：

* 代码块
* 行内代码
* API 路径
* 命令行内容
* 技术变量名称

---

## 6.3 字号规范

```css
--blog-font-size-xs: 12px;
--blog-font-size-sm: 14px;
--blog-font-size-base: 16px;
--blog-font-size-lg: 18px;
--blog-font-size-xl: 20px;
--blog-font-size-2xl: 24px;
--blog-font-size-3xl: 32px;
--blog-font-size-4xl: 40px;
```

正文默认：

```css
font-size: 16px;
line-height: 1.8;
```

移动端正文不得小于 15px。

---

# 7. 间距、圆角和阴影

## 7.1 间距系统

统一使用 4px 倍数间距：

```css
--blog-space-1: 4px;
--blog-space-2: 8px;
--blog-space-3: 12px;
--blog-space-4: 16px;
--blog-space-5: 20px;
--blog-space-6: 24px;
--blog-space-8: 32px;
--blog-space-10: 40px;
--blog-space-12: 48px;
--blog-space-16: 64px;
```

不得随意使用大量不规则间距值。

---

## 7.2 圆角

```css
--blog-radius-sm: 4px;
--blog-radius-md: 8px;
--blog-radius-lg: 12px;
--blog-radius-full: 9999px;
```

使用规则：

* 按钮：8px
* 输入框：8px
* 代码块：8px
* 提示块：8px
* 文章列表项：不强制卡片化
* 头像：圆形
* 标签：胶囊形或 4px 小圆角

不得使用过大的 20px 以上圆角制造移动应用感。

---

## 7.3 阴影

只在必要位置使用柔和阴影：

```css
--blog-shadow-1:
  0 1px 2px rgba(0, 0, 0, 0.04),
  0 1px 6px rgba(0, 0, 0, 0.03);

--blog-shadow-nav:
  0 1px 0 rgba(0, 0, 0, 0.08);
```

适用位置：

* 固定导航栏
* 移动端抽屉
* 浮动目录
* 弹出菜单

普通文章列表不使用明显阴影。

---

# 8. 响应式断点

统一使用以下断点：

```css
--blog-breakpoint-mobile: 640px;
--blog-breakpoint-tablet: 768px;
--blog-breakpoint-desktop: 960px;
--blog-breakpoint-wide: 1280px;
```

布局行为：

| 屏幕宽度         | 布局     |
| ------------ | ------ |
| 小于 640px     | 手机布局   |
| 640px～959px  | 平板布局   |
| 960px～1279px | 普通桌面布局 |
| 1280px 及以上   | 宽屏桌面布局 |

不得只针对单一桌面尺寸设计。

---

# 9. 页面整体布局

## 9.1 页面结构

博客前台统一结构：

```text
App
├── GlobalHeader
├── MainContent
│   ├── PageContent
│   └── OptionalAside
└── GlobalFooter
```

---

## 9.2 内容宽度

全局容器：

```css
max-width: 1152px;
margin: 0 auto;
padding-left: 24px;
padding-right: 24px;
```

文章详情页正文宽度：

```css
max-width: 760px;
```

文章正文不得无限延伸到超宽屏幕两侧。

移动端页面左右内边距：

```css
padding-left: 20px;
padding-right: 20px;
```

最小可以降低到 16px，但不得贴边。

---

# 10. 顶部导航栏

## 10.1 基础布局

导航栏固定在页面顶部。

```text
┌────────────────────────────────────────────┐
│ Logo/站名     首页  归档       GitHub  ◐   │
└────────────────────────────────────────────┘
```

导航栏要求：

* 高度 64px
* 桌面端横向布局
* 内容居中并限制最大宽度
* 背景使用半透明页面背景
* 页面滚动时保持固定
* 使用轻微背景模糊
* 底部使用 1px 分隔线
* `z-index` 高于正文和目录
* 不遮挡文章锚点标题

建议样式：

```css
background: color-mix(
  in srgb,
  var(--blog-c-bg) 85%,
  transparent
);
backdrop-filter: blur(12px);
```

必须提供不支持 `backdrop-filter` 时的普通背景降级方案。

---

## 10.2 站点标识

左侧显示：

* 可选的小型 Vue 风格几何图标
* 博客名称
* 点击后返回首页

要求：

* 图标不超过 28px
* 博客名称不超过一行
* 不使用大型 Vue 官方 Logo
* 不直接复制官方商标组合
* 可以使用自定义字母或几何标识

---

## 10.3 桌面导航

默认导航项：

* 首页
* 归档
* 关于

可选导航项：

* GitHub 外链

当前页面对应导航项需要有激活状态：

* 文字颜色变为品牌色
* 或底部出现 2px 品牌色线条

不得同时使用强背景、粗边框和下划线三种强调方式。

---

## 10.4 主题切换按钮

导航栏右侧提供主题切换按钮。

状态：

* 跟随系统
* 明亮模式
* 暗黑模式

MVP 可以采用简单的明暗两态切换，但必须：

* 首次访问优先读取系统主题
* 用户主动切换后保存在 localStorage
* 页面刷新后保持选择
* 页面初始加载不得明显闪烁错误主题
* 按钮具有可访问名称
* 不只显示一个无法理解的装饰图标

推荐存储键：

```text
blog-theme
```

值：

```text
light
dark
system
```

---

## 10.5 移动端导航

屏幕小于 768px 时：

* 隐藏桌面导航项
* 保留站点名称
* 保留主题按钮
* 显示菜单按钮
* 点击后打开右侧抽屉或顶部展开菜单

移动导航中至少包含：

* 首页
* 归档
* 关于
* 后台入口，可选

打开菜单后：

* 页面背景不可继续误触
* 支持 Escape 关闭
* 点击导航项后自动关闭
* 焦点不可丢失
* 不出现横向滚动

---

# 11. 博客首页

## 11.1 首页定位

首页是文章入口页面，不设计成复杂营销落地页。

首页应包含：

1. 简洁个人介绍区
2. 文章列表
3. 分页
4. 页脚

---

## 11.2 首页介绍区

结构示例：

```text
你好，我是某某
记录开发、工程实践与个人思考。

[阅读文章] [GitHub]
```

要求：

* 桌面端顶部间距 64～96px
* 标题最多两行
* 描述最多三行
* 主要按钮不超过两个
* 不使用全屏 Hero
* 不使用巨型背景图
* 不使用自动播放轮播图
* 不使用复杂渐变动画

推荐标题样式：

```css
font-size: clamp(32px, 5vw, 48px);
font-weight: 700;
line-height: 1.2;
letter-spacing: -0.02em;
```

标题中可使用一小段品牌色文字，但不得整段使用绿色。

---

## 11.3 文章列表标题

文章区顶部显示：

```text
最新文章
```

右侧可以显示：

```text
查看全部 →
```

“查看全部”链接到归档页。

---

## 11.4 文章列表项

文章列表采用文档式列表，不采用厚重卡片网格。

结构：

```text
文章标题
文章摘要，最多显示两到三行。
2026-08-01 · 5 条评论

────────────────────────────
```

每项包含：

* 标题
* 摘要
* 发布时间
* 评论数量
* 可选阅读时间
* 可选标签

交互要求：

* 点击标题进入详情
* 整行可点击属于可选优化
* 悬停时标题变为品牌色
* 分隔线使用弱边框
* 项目上下间距 24～32px
* 标题不超过两行
* 摘要不超过三行
* 长文本必须截断或自然换行
* 不在列表加载文章完整正文

不得为每篇文章添加：

* 大面积封面图
* 粗重阴影
* 多层嵌套卡片
* 五种以上元数据
* 大型“阅读全文”按钮

---

## 11.5 首页空状态

没有已发布文章时显示：

```text
暂时还没有文章
新的内容正在准备中。
```

要求：

* 居中或靠左均可
* 不显示错误样式
* 不出现空白页面
* 不使用复杂插画
* 可提供刷新按钮，但不是必需

---

## 11.6 首页分页

分页放在文章列表底部。

风格要求：

* 接近文档站点的轻量分页
* 当前页使用品牌色弱背景
* 非当前页透明背景
* 按钮最小点击区域 36px
* 禁用状态清晰
* 移动端可以只显示上一页、页码和下一页

分页切换后：

* 页面滚动至文章列表顶部
* 显示加载状态
* 请求失败显示明确提示
* 不保留上一页的错误数据状态

---

# 12. 归档页面

## 12.1 页面目的

归档页用于按时间快速查找文章。

路由建议：

```text
/archive
```

---

## 12.2 页面结构

```text
归档

2026
  08-01  文章标题
  07-20  文章标题

2025
  12-10  文章标题
```

要求：

* 按年份分组
* 年份倒序
* 年份内文章按发布时间倒序
* 每项显示月日和标题
* 标题悬停使用品牌色
* 不显示文章完整摘要
* 不设计复杂时间轴动画

如果后端没有专用归档接口，可以复用文章分页接口，但不得一次性无限请求全部数据。

MVP 可以先使用现有分页列表，以“全部文章”页面代替完整年份归档。

---

# 13. 文章详情页

## 13.1 桌面布局

宽屏布局：

```text
┌─────────────────────────────────────────────┐
│                 顶部导航                    │
├─────────────────────────────────────────────┤
│           文章正文区域        本页目录       │
│           最大 760px          200px          │
└─────────────────────────────────────────────┘
```

正文和目录之间间距至少 48px。

---

## 13.2 文章头部

文章头部显示：

* 文章标题
* 发布时间
* 最后更新时间，可选
* 评论数量
* 返回文章列表入口，可选

示例：

```text
使用 Vue 3 搭建个人博客

2026年8月1日 · 5 条评论
```

要求：

* 标题为页面唯一一级标题
* 桌面端标题 36～40px
* 移动端标题 28～32px
* 元数据使用次级文字颜色
* 不重复显示多个相同标题
* 不显示后台状态字段
* 不显示数据库 ID

---

## 13.3 正文排版

Markdown 正文统一使用 `.article-content` 根类。

推荐基础样式：

```css
.article-content {
  color: var(--blog-c-text-1);
  font-size: 16px;
  line-height: 1.8;
  overflow-wrap: anywhere;
}
```

---

## 13.4 标题层级

### 一级标题

文章正文原则上不再使用一级标题。

如果 Markdown 中存在一级标题，需要样式兼容，但不得比页面文章标题更强。

### 二级标题

```css
font-size: 24px;
font-weight: 600;
margin-top: 48px;
margin-bottom: 16px;
padding-top: 24px;
border-top: 1px solid var(--blog-c-divider);
```

第一个二级标题可以不显示顶部边框。

### 三级标题

```css
font-size: 20px;
font-weight: 600;
margin-top: 32px;
margin-bottom: 12px;
```

### 四级标题

```css
font-size: 17px;
font-weight: 600;
margin-top: 24px;
margin-bottom: 8px;
```

所有标题：

* 自动生成锚点 ID
* 支持目录跳转
* 锚点跳转后不得被固定导航遮挡
* 推荐设置 `scroll-margin-top: 88px`
* 悬停时可显示轻量锚点符号
* 锚点符号不得影响标题换行

---

## 13.5 段落

段落规则：

* 段落间距 16px
* 行高约 1.8
* 中文段落不强制首行缩进
* 不使用两端对齐
* 英文和长 URL 能够换行
* 单行长度保持适合阅读

---

## 13.6 链接

正文链接：

* 使用品牌色
* 默认不使用粗体
* 悬停时出现下划线
* 聚焦状态清晰
* 外部链接可以显示小型外链图标
* 外部链接使用安全的 `rel` 属性

不得仅使用颜色变化而无任何悬停或聚焦反馈。

---

## 13.7 行内代码

```css
.article-content :not(pre) > code {
  padding: 2px 6px;
  border-radius: 4px;
  background: var(--blog-c-code-inline-bg);
  color: var(--blog-c-text-1);
  font-size: 0.875em;
}
```

行内代码不得使用与普通链接相同的样式。

---

## 13.8 代码块

代码块要求：

* 使用暗色或随主题变化的代码背景
* 圆角 8px
* 上下间距 20～24px
* 横向溢出时允许滚动
* 不强制代码自动换行
* 移动端不得撑破页面
* 保留语法高亮
* 提供语言标识属于可选功能
* 提供复制按钮属于推荐功能

复制按钮要求：

* 默认弱化显示
* 鼠标悬停代码块时增强显示
* 点击后提示“已复制”
* 复制失败显示“复制失败”
* 移动端始终可访问
* 不引入大型代码编辑器依赖

---

## 13.9 引用块

```css
border-left: 3px solid var(--blog-c-brand-1);
padding: 8px 16px;
margin: 20px 0;
color: var(--blog-c-text-2);
background: var(--blog-c-brand-soft);
```

引用块不得使用厚重卡片阴影。

---

## 13.10 列表

有序和无序列表：

* 左侧缩进合理
* 列表项间距 6～8px
* 嵌套列表层级清楚
* 不能移除所有默认列表标记
* 移动端不得产生横向溢出

---

## 13.11 表格

Markdown 表格要求：

* 容器允许横向滚动
* 表头使用柔和背景
* 单元格使用 1px 边框
* 内容不强制压缩到不可阅读
* 移动端不得撑破正文容器

结构：

```html
<div class="table-wrapper">
  <table>...</table>
</div>
```

---

## 13.12 图片

文章图片要求：

* 最大宽度 100%
* 高度自动
* 居中显示
* 圆角不超过 8px
* 保留替代文本
* 加载失败不破坏布局
* 可使用原生懒加载
* 图片下方说明使用次级文字

当前项目不新增图片上传功能，只处理文章中已有的远程图片或 Markdown 图片。

---

## 13.13 提示块

支持以下视觉提示类型：

* 提示
* 信息
* 警告
* 危险

示例：

```text
TIP
这是补充说明。
```

实现方式可以是：

* 识别现有 Markdown 扩展语法；或
* 只提供对应 CSS 类供以后使用。

本期不要求重构 Markdown 解析器来增加复杂插件。

---

# 14. 文章目录

## 14.1 桌面端目录

当屏幕宽度大于等于 1280px 时，在正文右侧显示文章目录。

目录包含：

* 二级标题
* 三级标题

默认不显示四级及以下标题。

结构：

```text
本页目录

介绍
项目结构
  前端
  后端
部署方式
```

---

## 14.2 目录行为

目录要求：

* 使用 `position: sticky`
* 顶部偏移约 88px
* 最大高度不超过视口
* 内容过多时可滚动
* 当前阅读标题使用品牌色
* 其他标题使用次级文字颜色
* 点击后平滑滚动到对应标题
* URL Hash 正确更新属于推荐功能
* 刷新含 Hash 的地址可以定位到标题

目录滚动监听优先使用 `IntersectionObserver`。

不得为滚动监听注册大量未清理的事件。

---

## 14.3 中等屏幕

960px～1279px：

* 隐藏固定右侧目录
* 可在文章头部提供“本页目录”折叠按钮
* 折叠目录属于推荐功能，不是强制功能

---

## 14.4 移动端目录

小于 960px：

* 默认不占据正文宽度
* 可以通过按钮打开抽屉目录
* 也可以完全隐藏目录

如果实现移动目录：

* 点击标题后关闭抽屉
* 不遮挡导航栏
* 支持键盘和触摸操作
* 不影响浏览器返回行为

---

# 15. 文章底部区域

文章正文结束后显示：

1. 分隔线
2. 文章发布时间或更新时间
3. 返回文章列表
4. 评论区域

可选增加上一篇和下一篇文章，但本期不强制。

不得增加：

* 点赞
* 收藏
* 打赏
* 分享海报
* 相关推荐算法
* 复杂作者名片

除非现有项目已经实现这些功能。

---

# 16. 评论区域

## 16.1 评论区域标题

显示：

```text
评论 5
```

没有评论时显示：

```text
评论
暂时还没有评论，欢迎留下第一条评论。
```

---

## 16.2 评论提交表单

字段：

* 昵称
* 邮箱
* 评论内容
* 提交按钮

布局：

桌面端：

```text
[昵称                  ] [邮箱，可选             ]

[评论内容                                     ]
[                                             ]

                                  [提交评论]
```

移动端：

* 昵称和邮箱上下排列
* 提交按钮可以占满宽度

---

## 16.3 输入框风格

输入框使用轻量文档风格：

* 背景为页面背景或柔和背景
* 1px 边框
* 圆角 8px
* 高度至少 40px
* 聚焦时显示品牌色边框
* 聚焦时允许出现柔和外圈
* 错误时显示危险色
* 暗黑模式下边框清楚

不得直接使用浏览器默认输入框而没有统一样式。

---

## 16.4 评论按钮

主按钮：

```css
background: var(--blog-c-brand-1);
color: #ffffff;
border-radius: 8px;
min-height: 40px;
padding: 0 18px;
```

状态：

* 默认
* 悬停
* 聚焦
* 加载
* 禁用

提交中：

* 按钮禁用
* 文案改为“提交中…”
* 不允许重复提交
* 成功后清空评论内容
* 是否保留昵称和邮箱按现有逻辑处理

---

## 16.5 评论列表

单条评论结构：

```text
访客昵称                         2026-08-01

评论正文内容。
```

要求：

* 评论之间使用分隔线
* 不使用厚重卡片
* 昵称加粗但不夸张
* 日期使用次级文字
* 评论正文允许自然换行
* 长 URL 可换行
* 评论 HTML 不得直接执行
* 不显示邮箱
* 不显示审核状态
* 不显示数据库 ID

---

## 16.6 评论状态反馈

评论提交成功后显示：

```text
评论已提交，审核通过后将会显示。
```

错误状态至少区分：

* 参数错误
* 提交过于频繁
* 网络错误
* 服务异常

不得所有错误统一显示“请求失败”。

---

# 17. 关于页面

建议增加或保留：

```text
/about
```

内容可以包括：

* 简短个人介绍
* 主要技术方向
* 博客用途
* GitHub 链接
* 联系方式，可选

页面采用文章正文排版，不设计成复杂个人简历。

如果当前系统没有关于页面数据接口，可以先使用前端静态内容。

不得为此新增后台内容管理模块。

---

# 18. 页脚

页脚位于页面底部。

内容建议：

```text
Copyright © 2026 博客名称
Built with Vue 3 and Go
```

可选显示：

* GitHub
* RSS，只有实际存在时才显示
* 备案信息，只有用户提供时才显示

页脚要求：

* 与正文之间保持至少 64px 间距
* 上方使用弱分隔线
* 字号 13～14px
* 使用次级文字色
* 高度简洁
* 不放置大量导航链接
* 不使用醒目背景色

年份应自动根据当前日期生成，不得写死。

---

# 19. 404 页面

404 页面风格接近文档站点。

内容：

```text
404

页面不存在

你访问的页面可能已被删除或地址有误。

[返回首页]
```

要求：

* 不显示服务器内部信息
* 不显示默认 Nginx 404
* 不显示默认 Vue Router 空白页
* 返回首页按钮可用
* 明暗主题均正常

---

# 20. 加载、空状态与错误状态

## 20.1 页面加载

文章列表加载时：

* 显示轻量骨架屏或加载文字
* 不使用全屏遮罩
* 不反复闪烁
* 加载完成后平滑替换

文章详情加载时：

* 标题和正文区域可以显示骨架
* 不显示上一篇文章的残留内容

---

## 20.2 空状态

统一空状态组件：

```text
暂无内容
```

可带一行辅助说明。

不要为每个空状态使用不同风格。

---

## 20.3 错误状态

页面请求失败时显示：

* 简短错误标题
* 可理解的说明
* 重试按钮
* 返回首页入口，可选

错误状态不得只显示后端原始错误对象。

---

# 21. 明暗主题要求

## 21.1 覆盖范围

明暗主题必须覆盖：

* 页面背景
* 导航栏
* 页脚
* 文字
* 链接
* 分隔线
* 输入框
* 按钮
* 代码块
* 行内代码
* 引用块
* 表格
* 评论区域
* 目录
* 404 页面
* 加载和错误状态

不得只切换 body 背景而遗漏子组件。

---

## 21.2 图片和图标

暗黑主题下：

* 普通图片不强制反色
* 单色图标应使用 `currentColor`
* Logo 必须保证对比度
* 不使用只适合白色背景的固定黑色图标

---

## 21.3 主题过渡

允许使用：

```css
transition:
  color 0.25s,
  background-color 0.25s,
  border-color 0.25s;
```

不得对页面所有元素使用 `transition: all`。

主题切换不得使用持续时间超过 500ms 的复杂动画。

---

# 22. 交互和动效

## 22.1 动效原则

动效只用于：

* 按钮悬停
* 链接状态
* 导航菜单展开
* 主题切换
* 错误和成功提示
* 目录激活项

持续时间：

```text
150ms～250ms
```

---

## 22.2 禁止动效

不得增加：

* 页面进入飞入动画
* 文章卡片逐个弹跳
* 大面积视差滚动
* 鼠标跟随效果
* 粒子背景
* 无限旋转 Logo
* 自动播放轮播图
* 干扰正文阅读的悬浮动画

---

## 22.3 减少动画偏好

当用户系统启用减少动态效果时：

```css
@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    scroll-behavior: auto !important;
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}
```

---

# 23. 可访问性要求

必须满足：

* 页面使用语义化 HTML
* 页面只有一个主要 `h1`
* 标题层级不跳跃
* 导航使用 `nav`
* 主内容使用 `main`
* 页脚使用 `footer`
* 按钮使用 `button`
* 链接使用 `a` 或 RouterLink
* 图标按钮具有 `aria-label`
* 表单字段具有关联标签
* 错误提示可以被辅助技术识别
* 键盘可以访问所有交互控件
* 焦点状态清晰可见
* 颜色对比度足够
* 图片具有 `alt`
* 装饰图片使用空 `alt`
* 弹出菜单支持 Escape 关闭

不得通过可点击的 `div` 代替按钮。

---

# 24. SEO 与页面信息

在不改变后端架构的前提下实现基础页面信息。

首页：

```text
<title>博客名称</title>
<meta name="description" content="博客简介">
```

文章页：

```text
<title>文章标题 | 博客名称</title>
<meta name="description" content="文章摘要">
```

404 页面：

```text
<title>页面不存在 | 博客名称</title>
```

要求：

* 路由切换时更新标题
* 文章加载成功后更新标题
* 页面离开后不得保留上一文章标题
* 不引入大型 SEO 插件
* 不要求 SSR
* 不要求动态 Open Graph 服务端渲染

---

# 25. 前端组件规划

建议新增或整理以下组件：

```text
src/
├── components/
│   ├── common/
│   │   ├── AppButton.vue
│   │   ├── AppIconButton.vue
│   │   ├── AppLoading.vue
│   │   ├── AppEmpty.vue
│   │   └── AppError.vue
│   ├── layout/
│   │   ├── BlogHeader.vue
│   │   ├── BlogFooter.vue
│   │   ├── MobileNav.vue
│   │   └── ThemeToggle.vue
│   ├── article/
│   │   ├── ArticleList.vue
│   │   ├── ArticleListItem.vue
│   │   ├── ArticleMeta.vue
│   │   ├── ArticleContent.vue
│   │   ├── ArticleOutline.vue
│   │   └── CodeCopyButton.vue
│   └── comment/
│       ├── CommentForm.vue
│       ├── CommentList.vue
│       └── CommentItem.vue
├── layouts/
│   ├── BlogLayout.vue
│   └── AdminLayout.vue
├── styles/
│   ├── variables.css
│   ├── base.css
│   ├── typography.css
│   ├── markdown.css
│   ├── utilities.css
│   └── element-plus.css
└── composables/
    ├── useTheme.ts
    └── useArticleOutline.ts
```

这只是建议结构。

如果现有项目已有相似组件，应优先复用和调整，不得为了完全匹配目录而重复创建组件。

---

# 26. CSS 组织规范

## 26.1 全局样式职责

### variables.css

只存放：

* 颜色变量
* 字体变量
* 间距变量
* 圆角变量
* 阴影变量
* 层级变量

### base.css

存放：

* Reset
* HTML
* Body
* 通用元素
* 选中文字
* 滚动行为

### typography.css

存放：

* 基础标题
* 段落
* 链接
* 通用文字工具类

### markdown.css

只负责文章正文中 Markdown 生成的 HTML。

### element-plus.css

只负责管理后台的 Element Plus 主题覆盖。

---

## 26.2 样式限制

不得：

* 在每个组件重复定义品牌颜色
* 大量使用内联 style
* 大量使用 `!important`
* 使用无法追踪的随机 z-index
* 在组件中重复实现相同按钮样式
* 同时引入多个 CSS 框架
* 同时使用 Tailwind 和完整自定义样式体系
* 复制 VitePress 全部源码和 CSS

允许参考布局思想，但应按照当前项目重新实现最小必要样式。

---

# 27. 管理后台视觉调整

管理后台不需要重做业务结构，只进行有限品牌统一。

## 27.1 后台主色

Element Plus 主色调整为：

```css
:root {
  --el-color-primary: #42b883;
}
```

需要同时提供对应深浅状态，或使用 Element Plus 主题机制自动生成。

---

## 27.2 后台布局

建议结构：

```text
┌──────────┬──────────────────────────────┐
│ 侧边导航 │ 顶部栏                       │
│          ├──────────────────────────────┤
│          │ 页面内容                     │
└──────────┴──────────────────────────────┘
```

要求：

* 左侧导航简洁
* 当前菜单使用绿色强调
* 页面背景使用柔和灰色
* 内容区使用白色或暗色面板
* 表格、表单和弹窗继续使用 Element Plus
* 不将后台设计成 VitePress 文章页
* 不影响现有文章和评论操作

---

## 27.3 后台明暗模式

本期后台暗黑模式属于可选功能。

如果现有后台不支持暗黑模式：

* 前台必须支持明暗主题
* 后台可以暂时保持明亮主题
* 从前台进入后台时不得导致页面不可读

不得为了后台暗黑模式延迟前台改版交付。

---

# 28. 性能要求

## 28.1 依赖控制

不得为了纯视觉效果引入：

* 大型动画库
* 大型图标全集
* 第二套 UI 组件库
* 完整 VitePress 包
* 富文本编辑器
* CSS-in-JS 框架
* 复杂主题框架

图标应：

* 使用现有图标库
* 或按需引入
* 或使用少量内联 SVG

---

## 28.2 图片和资源

* 图片启用懒加载
* SVG 图标尽量复用
* 不加载大型背景图
* 不加载无用途字体
* 不使用超过需要尺寸的 Logo
* 构建产物不得因本次改版异常增长

---

## 28.3 运行表现

目标：

* 页面滚动平滑
* 目录监听不造成明显卡顿
* 主题切换无明显布局跳动
* 移动菜单开关流畅
* 评论表单输入无明显延迟
* Markdown 长文章不会导致页面失去响应

---

# 29. 兼容性要求

至少支持：

* Chrome 最新稳定版
* Edge 最新稳定版
* Firefox 最新稳定版
* Safari 最近两个主要版本，建议

移动端至少检查：

* iOS Safari
* Android Chrome

如果某项视觉效果在浏览器中不支持，应提供可用降级，不得导致功能不可用。

---

# 30. 本次不实现的内容

为了控制改版范围，本次不实现：

* 将项目迁移为 VitePress
* 服务端渲染
* 静态站点生成
* 全文搜索
* Algolia
* 标签系统
* 分类系统
* 图片上传
* 文章封面管理
* 用户头像上传
* 评论回复
* 点赞
* 收藏
* 分享海报
* 阅读进度条
* 复杂转场动画
* 自定义主题编辑后台
* 多套主题皮肤
* 多语言
* PWA
* 离线阅读
* RSS
* 文章推荐算法
* 首页轮播图

不得自行增加上述功能。

---

# 31. 功能兼容要求

界面改版后，以下现有功能必须保持可用：

* 已发布文章列表
* 文章分页
* 文章详情
* Markdown 渲染
* 评论列表
* 评论提交
* 评论审核逻辑
* 管理员登录
* 后台统计
* 文章创建
* 文章编辑
* 草稿发布
* 发布转草稿
* 文章删除
* 评论审核
* 评论拒绝
* 评论删除
* JWT 过期处理
* Vue Router 刷新
* Docker 构建
* Nginx 路由

本次改版不得修改后端接口，除非现有接口无法提供页面必需数据。

如确需修改接口，必须先记录：

* 当前缺失数据
* 使用场景
* 最小接口变更
* 兼容性影响

---

# 32. 页面验收标准

## 32.1 全局导航

* [ ] 导航栏固定在顶部。
* [ ] 导航栏不会遮挡文章标题锚点。
* [ ] Logo 和博客名称清晰。
* [ ] 当前导航项状态明确。
* [ ] 主题切换可用。
* [ ] 移动端菜单可用。
* [ ] 页面滚动时导航栏不抖动。
* [ ] 明暗主题下导航栏均可读。

---

## 32.2 首页

* [ ] 首页具有简洁介绍区。
* [ ] 文章列表采用文档式布局。
* [ ] 文章标题、摘要和时间层级清楚。
* [ ] 文章列表不显示完整正文。
* [ ] 分页功能正常。
* [ ] 无文章时显示空状态。
* [ ] 加载失败时显示重试状态。
* [ ] 手机端无横向滚动。

---

## 32.3 文章详情

* [ ] 正文宽度适合长时间阅读。
* [ ] 标题层级清楚。
* [ ] 代码块可横向滚动。
* [ ] 行内代码样式清楚。
* [ ] 表格不会撑破页面。
* [ ] 图片不会撑破页面。
* [ ] 链接状态清晰。
* [ ] Markdown XSS 清理仍然有效。
* [ ] 桌面端目录可正常定位。
* [ ] 当前目录项可以随滚动变化。
* [ ] 固定导航不会遮挡锚点标题。
* [ ] 移动端正文可读。

---

## 32.4 评论

* [ ] 评论表单样式统一。
* [ ] 表单错误提示清楚。
* [ ] 提交中不可重复点击。
* [ ] 成功提示说明需要审核。
* [ ] 公开评论不显示邮箱。
* [ ] 长评论不会破坏布局。
* [ ] 评论中的恶意 HTML 不执行。
* [ ] 移动端表单正常排列。

---

## 32.5 主题

* [ ] 明亮主题完整。
* [ ] 暗黑主题完整。
* [ ] 用户选择可以持久化。
* [ ] 首次访问可以跟随系统主题。
* [ ] 页面刷新无明显主题闪烁。
* [ ] 代码块在两种主题下均可读。
* [ ] 输入框在两种主题下边界清晰。
* [ ] 错误、警告和成功状态可区分。

---

## 32.6 后台

* [ ] Element Plus 主色统一为 Vue 绿色。
* [ ] 后台现有功能没有被破坏。
* [ ] 表格、表单和对话框显示正常。
* [ ] 当前菜单项状态明确。
* [ ] 删除确认仍然正常。
* [ ] 登录页风格与博客品牌基本一致。

---

# 33. 响应式验收尺寸

至少在以下尺寸测试：

```text
375 × 812
390 × 844
768 × 1024
1024 × 768
1366 × 768
1440 × 900
1920 × 1080
```

每个尺寸必须检查：

* 是否存在横向滚动
* 导航是否溢出
* 标题是否异常换行
* 正文是否过宽或过窄
* 代码块是否可滚动
* 表格是否可滚动
* 评论表单是否溢出
* 目录是否按规则显示或隐藏
* 页脚是否异常悬空
* 按钮是否容易点击

---

# 34. 测试命令

完成改版后必须执行：

```bash
cd frontend
npm run typecheck
npm run build
```

如果项目存在测试命令：

```bash
npm run test
```

项目根目录执行：

```bash
docker compose build
docker compose up -d
docker compose ps
```

必须实际打开浏览器检查：

* 首页
* 文章详情
* 归档页
* 关于页
* 404 页面
* 管理员登录页
* 后台文章页
* 后台评论页
* 明亮主题
* 暗黑主题
* 移动端布局

不得仅凭构建成功宣称界面验收通过。

---

# 35. Codex 实施任务

为了减少 Codex 额度消耗，每次只执行一个任务。

## UI-01：设计变量与全局基础样式

完成：

* 读取现有前端结构
* 增加颜色变量
* 增加明暗主题变量
* 增加字体和间距变量
* 整理全局基础样式
* 实现主题状态管理
* 实现主题切换按钮
* 不修改具体业务页面布局

验收：

* 明暗主题切换正常
* 刷新后主题保持
* 页面基础颜色正常
* 前端构建通过

---

## UI-02：顶部导航与页脚

完成：

* BlogHeader
* 桌面导航
* 移动导航
* ThemeToggle
* BlogFooter
* 固定导航
* 当前路由状态
* 响应式菜单

不要改文章详情和评论样式。

---

## UI-03：首页与归档页

完成：

* 首页介绍区
* 文档式文章列表
* 文章列表项
* 加载状态
* 空状态
* 错误状态
* 分页
* 归档页或全部文章页

保持现有 API 不变。

---

## UI-04：文章详情与 Markdown 排版

完成：

* 文章头部
* 正文容器
* Markdown 标题
* 段落
* 链接
* 列表
* 引用
* 代码块
* 行内代码
* 表格
* 图片
* 文章目录
* 锚点滚动
* 复制代码按钮，推荐

不得降低现有 XSS 防护。

---

## UI-05：评论、404 和状态页面

完成：

* 评论表单
* 评论列表
* 评论状态提示
* 404 页面
* 加载组件
* 空状态组件
* 错误组件
* 移动端适配

不得修改评论审核业务逻辑。

---

## UI-06：后台品牌统一与最终回归

完成：

* 后台主色统一
* 登录页样式调整
* 后台导航状态优化
* 检查 Element Plus 组件
* 全尺寸响应式检查
* 明暗主题检查
* 前端构建
* Docker 构建
* 核心业务回归

不得重做后台业务结构。

---

# 36. 首次交给 Codex 的提示词

```text
请先完整阅读：

1. docs/REQUIREMENTS.md
2. docs/FRONTEND_UI_REQUIREMENTS.md
3. docs/TEST_REQUIREMENTS.md

当前项目已经完成博客核心功能。本次任务只进行前端视觉改版。

请注意：
- 不要将项目迁移到 VitePress。
- 只实现类似 Vue 官方文档和 VitePress 默认主题的视觉风格。
- 不修改 Go 后端和现有 API。
- 不增加需求之外的功能。
- 不进行无关重构。

现在只执行 UI-01：设计变量与全局基础样式。

要求：
1. 先检查当前前端目录、组件和样式。
2. 优先复用现有文件。
3. 实现统一 CSS 变量和明暗主题。
4. 不提前修改首页、文章页和评论页布局。
5. 实际运行 npm run typecheck 和 npm run build。
6. 最后只报告：
   - 修改文件
   - 实现内容
   - 执行命令
   - 验证结果
   - 未解决问题
```

---

# 37. 后续 Codex 提示词模板

```text
请读取：

- docs/REQUIREMENTS.md
- docs/FRONTEND_UI_REQUIREMENTS.md
- docs/TEST_REQUIREMENTS.md

现在只执行 UI-XX。

严格遵守任务边界：

1. 不迁移到 VitePress。
2. 不修改后端接口。
3. 不增加需求外功能。
4. 优先复用现有组件和样式。
5. 不进行无关重构。
6. 完成后运行前端类型检查和构建。
7. 实际检查相关页面。
8. 最后只报告：
   - 修改文件
   - 完成内容
   - 验证命令
   - 验证结果
   - 遗留问题
```

将 `UI-XX` 替换为：

```text
UI-02
UI-03
UI-04
UI-05
UI-06
```

---

# 38. 单页面修复提示词

```text
请读取 docs/FRONTEND_UI_REQUIREMENTS.md。

只修复以下界面问题：

页面：
屏幕尺寸：
当前表现：
预期表现：
复现步骤：
截图信息：

限制：
1. 只修复该界面问题。
2. 不修改后端。
3. 不重构无关组件。
4. 不引入新的 UI 框架。
5. 保持明暗主题兼容。
6. 保持移动端兼容。
7. 修复后运行 npm run typecheck 和 npm run build。
8. 最后只报告根因、修改文件和验证结果。
```

---

# 39. 完成定义

只有满足以下条件，本次前端改版才视为完成：

1. 前台整体呈现 Vue/VitePress 风格。
2. 现有项目仍然是 Vue 动态应用。
3. 明亮和暗黑主题均可用。
4. 首页采用轻量文档式文章列表。
5. 文章详情具有良好的 Markdown 阅读体验。
6. 桌面端文章目录可用。
7. 评论区域风格统一。
8. 手机、平板和桌面布局正常。
9. 后台保留 Element Plus 并完成品牌色统一。
10. 原有业务功能没有被破坏。
11. TypeScript 类型检查通过。
12. 前端构建通过。
13. Docker 构建通过。
14. 不存在明显横向溢出。
15. 不存在严重明暗主题显示错误。
16. 不存在新增的 XSS 风险。
17. 不存在需求范围外的大型功能。
